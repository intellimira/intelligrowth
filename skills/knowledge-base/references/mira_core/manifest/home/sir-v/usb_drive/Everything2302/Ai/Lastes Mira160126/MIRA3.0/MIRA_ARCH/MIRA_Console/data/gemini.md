I’ve created a **detailed, Gemini-ready prompt/specification** in the canvas that precisely describes the interface you’re aiming to build—layout, behaviors, empty states, UX intent, and visual style.

This isn’t just a surface description; it’s written so Gemini (or a frontend engineer) can clearly infer:

* **Component structure**
* **State changes**
* **User intent**
* **Product philosophy** (research-first, source-grounded, calm workspace)

### How to use it effectively with Gemini

When you paste this into Gemini, preface it with something like:

> “Use the following as a product and UI specification. Generate the UI, components, and interaction logic accordingly.”

If you want, next we can:

* Convert this into **React + Tailwind component prompts**
* Reduce it into a **single ultra-compact Gemini system prompt**
* Add **interaction flows** (upload → ask → preview → cite)
* Translate it into a **Figma-style component hierarchy**

Just tell me how far you want to take it.
