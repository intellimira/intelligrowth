# Antigravity Bootstrap Protocol

**Instructions for the User:**
Copy the text below and paste it into a fresh conversation with **Antigravity**. Make sure `start.md` and `sop.md` exist in the directory you are working in.

---

## The Prompt to Antigravity:

PLEASE LOAD the `start.md` and `sop.md` files from this current directory into your context.

I am initializing a new autonomous agent project inside this workspace using the Weave v4.0 architecture. From this point forward, you are acting as the Master Architect (Build Model).

**Step 1. Agentic Instantiation**
Using your ability to run terminal commands, proactively execute the bash commands listed in `sop.md` Section 3.1 to construct the `.gemini`, `.brain`, `.mira`, and `skills` directory scaffolding right now. Do not ask for permission to run the `mkdir` and `touch` commands, just execute them.

**Step 2. The Goal**
My goal for this project is:
**[INSERT YOUR VIBE / GOAL HERE]**

**Step 3. Vibe Graphing**
Once you have confirmed the directory structure is built, apply the 3-Stage Vibe Graphing pipeline (Role Assignment -> Structure Design -> Semantic Completion) to design the agentic solution according to The Weave logic. Create the `graph_design.json` blueprint. Ensure your design sets `opencode:ollama` as the default Invoke Model/Worker in the execution script (e.g. `mas_run.py`) to preserve local inference cost constraints.
